[settings]
# Black-compatible import sorting, matching ruff's line length.
profile = black
line_length = 120

# Match the ruff excludes: never touch generated or vendored code.
extend_skip_glob = */pb/*,*/migrations/*
skip_gitignore = true
